# 开发环境：一律走 Vite 同源代理，cookie Path=/api 才能带上
# 后端默认 8124（见 vite.config.ts VITE_PROXY_TARGET）

# 部署链接（本地静态控制器同时读 code_deploy）
VITE_DEPLOY_DOMAIN=http://localhost:8124/api/static

# 空 = 预览用相对路径 /api/static/...
VITE_PREVIEW_DOMAIN=

# 空或 /api = 不跨端口，避免 SESSION 丢到 8124
VITE_API_BASE_URL=/api
